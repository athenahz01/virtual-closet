import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function TryOnPage() {
  return (
    <PhasePlaceholder
      title="Try-on atelier."
      description="Gemini try-on generation is wired server-side. The full outfit builder lands in the next checkpoint."
    />
  );
}
