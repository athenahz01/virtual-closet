import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function AvatarPage() {
  return (
    <PhasePlaceholder
      title="Avatar studio."
      description="Ready Player Me and the 3D viewer are queued for Phase 3, after the digital wardrobe foundation is in place."
    />
  );
}
