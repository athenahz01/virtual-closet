import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function CalendarPage() {
  return (
    <PhasePlaceholder
      title="Outfit calendar."
      description="Planning and wear history will live here in Phase 5, with the same private Supabase-backed data model."
    />
  );
}
