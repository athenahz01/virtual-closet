import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function OutfitsPage() {
  return (
    <PhasePlaceholder
      title="Outfit library."
      description="Saved generated looks, favorites, wear counts, and downloads arrive after try-on generation is live."
    />
  );
}
